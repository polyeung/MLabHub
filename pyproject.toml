[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "morality-game-mvp"
version = "1.0.0"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["MLabHub"]
